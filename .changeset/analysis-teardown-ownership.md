---
"@ifc-lite/viewer": patch
---

Closing the Clash panel now releases only the isolation or X-ray it set itself, so an isolation made elsewhere survives. Closing the BCF panel gives back the section cut (and tool) that viewpoints replaced, unless you changed the cut meanwhile.
