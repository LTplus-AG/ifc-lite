---
"@ifc-lite/clash": patch
"@ifc-lite/cli": patch
"@ifc-lite/viewer": patch
---

`runClash` now rejects a non-finite `settings.tolerance`, per-rule `rule.tolerance`, or `rule.clearance` (e.g. `NaN` from `Number('abc')` on a cleared input, or a unit-conversion division by zero) instead of letting `??` pass it through unchanged. A non-finite tolerance previously NaN-poisoned the broad-phase bounds, so the geometry pass silently examined zero candidate pairs while `ruleCoverage`'s selector-match counts still read as full coverage — `classifyRuleCoverage` reported `'clean'` having checked nothing.

`ClashRuleCoverage` also gains optional `candidatesProcessed` / `candidatesDropped` fields, threaded from the kernel's `RuleDetection`, so a caller can now tell "checked hundreds of pairs, found nothing" apart from "checked zero" — the distinction `matchedA`/`matchedB` alone (computed before geometry runs) could not make. Additive; existing consumers are unaffected.

`classifyRuleCoverage` now reads that new evidence: previously a rule with full selector coverage (`matchedA`/`matchedB` both non-zero) always classified as `'clean'` even when the geometry kernel examined zero candidate pairs for it — the exact shape a non-finite tolerance (or an exhausted `maxCandidatePairs` budget) produces. Such a rule now classifies as `'partial'` instead of `'clean'`; a rule that both matched its selectors and had pairs to examine still classifies as `'clean'`, and a rule with a genuinely empty side still classifies as `'no-match'`, unchanged. `ruleCoverageWasUnexamined` (the function distinguishing this case) is now exported from `@ifc-lite/clash`'s package root — it existed in `analysis.ts` but was missing from the barrel, so `import { ruleCoverageWasUnexamined } from '@ifc-lite/clash'` silently resolved to `undefined` and any call site threw a `TypeError` at runtime.

The CLI's `clash --matrix`/`--a`/`--b` human summary previously worded this new `'partial'` case identically to a plain empty selector — `"Note: N of M rule(s) never ran a comparison: "` — but the empty-selector describer (`ruleHadNoMatch`-based) never named an unexamined-but-matched rule, so the line's trailing list was silently blank for exactly the alarming case it exists to report. It now prints a separate `WARNING` line naming the unexamined rule(s) and explaining that a real comparison silently did not happen, while a genuinely empty-selector rule keeps its existing `Note` wording unchanged, and an ordinary clean run still prints nothing.

The viewer's clash panel had the same gap: its zero-clashes `'partial'` explanation was also built only from the empty-selector list, so a rule that matched but was never examined rendered no explanation at all — just the plain "No clashes found for this rule set. 🎉", false comfort for a comparison that silently didn't happen. The panel now shows a distinct warning naming the unexamined rule(s) in that case, keeps the existing empty-selector note unchanged when that's the only cause, and still shows the plain 🎉 message with no note on an ordinary clean run.
