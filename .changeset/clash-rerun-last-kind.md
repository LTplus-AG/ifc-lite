---
"@ifc-lite/viewer": patch
---

Clash panel: Re-run now repeats the run that produced the result on screen (Detect all, the rule set, one preset, or the duplicate scan) instead of always running "Detect all", and its tooltip names what it will repeat.
