---
'@ifc-lite/cli': minor
---

`ifc-lite flow <run|describe|validate>`: evaluate a `*.flow.json` node graph headlessly with the standard `@ifc-lite/flow-nodes` library — Player-style `--input` overrides, `--out` to write the model with the run's mutations, a describe schema for callers, and a per-node availability report.
