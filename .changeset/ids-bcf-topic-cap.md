---
"@ifc-lite/viewer": patch
---

IDS → BCF export: the dialog now defaults to one topic per failing specification, shows how many topics the chosen grouping creates, and warns before an export would pass the 1,000-topic cap. Snapshot capture stops at the cap instead of rendering every failing entity.
