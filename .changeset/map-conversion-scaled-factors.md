---
"@ifc-lite/parser": patch
"@ifc-lite/server-client": patch
---

Preserve the per-axis scale factors from `IfcMapConversionScaled` and apply them consistently in georeferencing transforms and exported placement metadata.
