---
"@ifc-lite/viewer": patch
---

Mobile: a Panels button opens a sheet listing every panel the desktop rail offers, so Clash, BCF, Lists, Script and the rest are reachable on a phone, not only Hierarchy and Properties. The list is the rail's own list, so hidden panels stay hidden (#5853).
