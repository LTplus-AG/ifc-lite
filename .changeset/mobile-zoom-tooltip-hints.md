---
"@ifc-lite/viewer": patch
---

Mobile: the zoom buttons' tooltips no longer name the + and − keys, which add to and remove from the basket rather than zoom. The tooltips now read "Zoom in" and "Zoom out" (#5846).
