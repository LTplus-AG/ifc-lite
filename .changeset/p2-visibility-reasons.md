---
"@ifc-lite/viewer": patch
---

Show All, Home, the `A` key and the context menu's Show all now run one reset, driven by a single table of every mechanism that can hide geometry. Hidden federated models now come back too; before, every reset left them hidden. The active lens, the class-type toggles, the Model/Types view mode and embed-host hidden classes are deliberately kept, and the table names them so the viewport can say so.
