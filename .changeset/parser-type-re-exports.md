---
"@ifc-lite/parser": minor
---

Re-export `EntityTable` and `SpatialHierarchy` types from `@ifc-lite/parser` for downstream consumer convenience. Previously these required a separate `@ifc-lite/data` import.
