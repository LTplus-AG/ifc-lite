---
'@ifc-lite/pointcloud': minor
---

Expose source-supplied PLY `nx`/`ny`/`nz` normals on decoded and streamed point chunks while preserving row order through bounded, stride-aware decoding and the worker wire protocol.
