# Interaction panel type scale (#5821)

Headless Chrome at 1440 × 900 loaded the authored `building-architecture.ifc` demo (12 geometric elements) on the parent and this branch. The [before](command-before.png) and [after](command-after.png) captures open the command palette over the same viewer state. Its Esc keycap computes at 10px before and 12px after; the palette layout and visible rows remain intact.

This stack applies the same 12px type token to small text in Chat, the tour card, the annotation drop form, and the zone apportionment summary.
