# Retained cold-load local validation

This records local CI validation of the retained implementation integrated with main at `5c75ee6339872fdac698594dc234aa6715d2d809`. It supplements the existing performance qualification; it introduces no new performance result or universal no-regression claim. Subsequent cache test-only commits add a finite completion bound and distinguish mesh payloads; their focused validation is recorded separately.

The completed Node/static, documentation, native workspace/feature and Python committed-reference lanes passed, subject to the explicit omissions below. The first triangulation census failed after a script test overwrote a fixture through a nested symlink. That failed log remains retained. Recorded timestamps place the corruption after the preceding native lanes completed; only the census verdict was affected. Fixtures were restored against official hashes and copied privately. The identical census command passed after restoration, including its coverage assertion; a subsequent full fixture-hash check passed. The failed original is not relabeled as successful.

Local execution used macOS arm64, Node 22.14.0 and pnpm 10.8.1. Rust workspace gates used the pinned nightly. The Python quick lane used stable Rust and Python 3.12, compiled the actual binding and compared committed references; it did not run the nightly full IfcOpenShell-engine lane. Its workflow command resolved the Python lockfile before building; the resolved lock and diff were retained and the tracked lock was restored afterward.

The full root build, typecheck, test and lint lanes and actual WASM boundary contracts passed. Existing ignored Rust tests and two viewer skips remain unchanged. The documentation checker retains its explicit illustrative-snippet exclusions. The integration harness reported success while explicitly skipping optional DuckDB initialization because that dependency was unavailable. Local validation does not relabel queued remote jobs, PR metadata checks or review signals as successful, and does not substitute for post-merge main monitoring.

`gates.jsonl` identifies commands, outcomes and SHA-256 hashes of retained private logs. Log contents are not published because they can contain local paths and model details. Archive identifiers are stable relative evidence identifiers, not public download links. The original failed and interrupted attempts remain retained. This report contains no private models, coordinates, property payloads, screenshots or profiles.
