//! Verbatim copies of the hand-written clash math modules from
//! /Users/louistrue/Development/ifc-lite-plato/rust/clash/src/.
//! Only the intra-crate `use crate::vec3::...` paths were rewritten to
//! `crate::reference::vec3::...` so the files compile standalone.

pub mod aabb;
pub mod triangle;
pub mod vec3;
